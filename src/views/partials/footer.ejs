</body>
<footer>
    <div class="container-footer">
        <div class="logo">
            <span class="cap">Sign up for The Pickle</span>

            <h5>Designed by Matthew Hoepfl</h5>
        </div>
        <div class="nav">
            <a 
            <% if (page_name === 'index') { %>
                class="active"
            <% } %>
            href="/">Home</a>
            <a 
            <% if (page_name === 'register') { %>
                class="active"
            <% } %>
            href="/register">Register</a>
            </div>
        </div>
    </footer>
</html>
